server:
  port: 9081
spring:
  application:
    name: TCS-NxtGen-MS1
  cloud:
    config:
      uri: http://localhost:9090/
      enabled: true
  profiles:
    active: subversion
management:
  security: 
    enabled: false